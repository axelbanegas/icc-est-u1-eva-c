// Clase que contiene la lógica para ordenar libros y buscar libros; incluye:
// - `sortByXxxxx(Book[] libros)`
// - `searchByXxxxx(Book[] libros, Xxxxx xxxxxx)`
package Controllers;
import Models.Book;

public class LibroController {

    public sortBybooks(Book[] libros){
        for(i = 0; i<= length(libros); i ++){
            if(getname().length()>)
            i = j;
            j = i+1;
        }
    }
    public searchBybooks(Book[] libros, String Libro){
        int bajo = 0;
        int alto = length(libros)-1;
        while(bajo<= alto){
            int center = (bajo+alto)/2;
            if(getname()length()>center){
                System.out.println("Libro no encontrado");
                return -1;
            }else{
                System.out.println("Libro encontrado");
                return libros[bajo]
            }

        }

    }
}
